<Window x:Class="Graphium.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Graphium"
        xmlns:c="clr-namespace:Graphium.Controls"
        mc:Ignorable="d"
        Title="Graphium" Height="800" Width="1280"
        Icon="/Graphium;component/Assets/favicon.ico"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New" Command="{Binding NewTabCmd}"/>
                    <MenuItem Header="_Preferences" Command="{Binding PreferencesCmd}"/>
                </MenuItem>
                <MenuItem Header="_Data Acquisition">
                    <MenuItem Header="_Setup..." Command="{Binding DataAcquisitionSetupCmd}"/>
                </MenuItem>
                <MenuItem Header="_Window">
                    <MenuItem Header="_Select Next Tab" Command="{Binding NextTabCmd}"/>
                    <MenuItem Header="_Select Previous Tab" Command="{Binding PreviousTabCmd}"/>
                </MenuItem>
            </Menu>

            <TabControl Margin="10"
                        ItemsSource="{Binding Tabs}"
                        SelectedItem="{Binding CurrentTab}">

                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="{Binding Name}" Margin="0,0,5,0"/>
                            <Button Content="×" Width="16" Height="16"
                                    Command="{Binding DataContext.CloseTabCmd, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                    CommandParameter="{Binding}"
                                    Padding="0"
                                    Margin="5,0,0,0"
                                    FontWeight="Bold"
                                    FontSize="12"
                                    HorizontalContentAlignment="Center"
                                    VerticalContentAlignment="Center"
                                    BorderThickness="0"/>

                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <c:MeasurementControl DataContext="{Binding}"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </DockPanel>
    </Grid>
</Window>
