<Window x:Class="Graphium.Views.DataAcquisitionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Graphium.ViewModels"
        xmlns:local="clr-namespace:Graphium.Views"
        xmlns:c="clr-namespace:Graphium.Controls"
        mc:Ignorable="d"
        Title="DataAcquisitionWindow" Height="450" Width="800">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"
                 ItemsSource="{Binding MenuItems}"
                 DisplayMemberPath="Header"
                 SelectedItem="{Binding CurrentMenuItem}"/>

        <ContentControl Grid.Column="1"
                        Content="{Binding CurrentMenuItem}"
                        Margin="5"/>
    </Grid>
</Window>
